# Installer Assets

This folder stores packaging assets used by the Windows installer flow.

- `jpackage/`: resource overrides consumed by `jpackage`
- generated icon files also land in `jpackage/`

The generated outputs themselves are written to `dist/`, not here.
